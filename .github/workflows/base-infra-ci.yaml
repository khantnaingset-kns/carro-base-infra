name: Terraform Base Infra CI

on:
  push:
  pull_request:
    branches:
      - main
      - features/**
    paths:
      - base-infra/**
      

jobs:
  terraform:
    name: "Terraform"
    uses: ./.github/workflows/tf-change-management.yaml