name: Terraform CI

on:
  push:
    branches:
      - main
      - features/**
  pull_request:
    branches:
      - main
      - features/**

jobs:
  terraform:
    uses: ./.github/workflows/tf_change_management.yaml
